language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
addons:
  apt:
    packages:
      - libopenblas-dev
      - gfortran
nightly:
  script:
    - cargo +nightly build --verbose --features nightly
    - cargo +nightly test --verbose --features nightly
